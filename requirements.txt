# WealthDesk -- Agentic AI Engineering, Batch 1
# Install once and keep for the full 17 sessions:
#   pip install -r requirements.txt
#
# All packages are included upfront to avoid mid-session install interruptions.
# Each group comment shows which session first uses that package.

# --- Sessions 1-17: Core LangGraph stack ---
langgraph>=1.0.0
langgraph-checkpoint-sqlite>=2.0.0    # SqliteSaver for multi-turn memory (US-02, Session 2)
aiosqlite>=0.19.0                     # async SQLite support required by langgraph-checkpoint-sqlite
langchain>=0.3.0
langchain-core>=0.3.0
langchain-text-splitters>=0.3.0        # RecursiveCharacterTextSplitter (split from langchain in 0.3)
langchain-groq>=0.1.9                  # Groq (meta-llama/llama-4-scout-17b-16e-instruct) -- the agent LLM

# --- Session 1+: Environment and runtime ---
python-dotenv>=1.0.0                   # reads .env -- first line of every agent file
pydantic>=2.0.0                        # state validation, structured outputs

# --- Session 4+: Vector store and RAG ---
chromadb>=0.5.0
langchain-chroma>=0.1.4
langchain-huggingface>=0.0.3           # free local embeddings (all-MiniLM-L6-v2)
sentence-transformers>=2.7.0           # backing library for HuggingFaceEmbeddings

# --- Session 6+: Evaluation (LLM-as-judge) ---
openai>=1.0.0                          # GPT-4o-mini as the eval judge (different provider from Groq)
langchain-openai>=0.1.0

# --- Session 9+: Observability ---
langsmith>=0.1.0

# --- Session 7+: MCP (Model Context Protocol) ---
mcp>=1.0.0

# --- Session 13+: Web UI ---
streamlit>=1.35.0

# --- Session 15+: Containerisation ---
# Docker is installed separately -- not a pip package.
# See s15/instructor_notes.md for Docker Desktop setup.

# --- Development and testing ---
pytest>=8.0.0
pytest-mock>=3.12.0
pytest-asyncio>=0.23.0                 # async test support (needed from S7 MCP onward)
httpx>=0.27.0                          # required by several LangChain integrations
